# Codex Skills (Memoria)

このリポジトリで使う Codex CLI の Skill 定義を置く場所です。

各スキルは `SKILL.md`（大文字）がエントリポイントになります。

記事の手順どおりに使う場合は、以下を満たしてください。

1. `~/.codex/config.toml` で `features.skills = true` を有効化
2. `~/.codex/skills` をこのフォルダ（`.codex/skills`）へリンク、またはコピー
